// import { test, expect } from '@playwright/test';

// test('test', async ({ page }) => {
//   test.slow()
//   await page.goto('https://cypress-training.iliad-solutions.com/reg/');
//   await page.getByRole('textbox', { name: 'Username:' }).click();
//   await page.getByRole('textbox', { name: 'Username:' }).fill('admin');
//   await page.getByLabel('Password:', { exact: true }).click();
//   await page.getByLabel('Password:', { exact: true }).fill('changeme');
//   await page.getByRole('button', { name: 'Login' }).click();
//   await page.getByText('MASTERCARD_R').click();
//   await page.getByRole('button', { name: 'OK' }).click();
//   await page.getByRole('button', { name: 'Create Role' }).click();
//   await page.getByLabel('Role Name:*:').fill('test');
//   await page.getByLabel('Description:').click();
//   await page.getByLabel('Description:').fill('test des');
//   await page.getByRole('button', { name: 'OK' }).click();
//   await page.getByText('test', { exact: true }).click();
//   await page.getByText('test des').click();
// });

// repository

// bit bucket
// github


// pull
// push


// for making any codebase as git repo , u have to intilaise it
// comamnd is git init


